# TODO.md — Ágora

Motor JIT: este documento mantiene **siempre exactamente 2 tareas atómicas** activas. Al completar cualquiera, se elimina, se mueve su resumen a `MEMORY.md` §2, y se calcula la siguiente tarea más prioritaria comparando `PRD.md` (roadmap) contra `MEMORY.md` (estado actual).

**Estado al cierre de sesión (25/08/2026, continuación):** **Sincronización con Google Calendar (roadmap #22) completa** — implementada y **validada en vivo en staging por el usuario** ("Efectivamente, ese era el problema. Ahora todo funciona bien."), tras una sesión de depuración con **4 bugs reales** encontrados y corregidos uno por uno en producción/staging real: (1) `.github/workflows/deploy.yml` nunca reenviaba el secreto `GOOGLE_CALENDAR_SERVICE_ACCOUNT` al paso `serverless deploy` (mismo tipo de gap ya documentado 3 veces antes en este proyecto, esta vez un nivel más arriba, en el workflow); (2) con ese fix ya desplegado, el deploy real fallaba con `Request must be smaller than 5120 bytes for the UpdateFunctionConfiguration operation` — límite duro de AWS Lambda para variables de entorno, resuelto migrando la credencial de Calendar a **AWS SSM Parameter Store** (nivel Standard, gratis, verificado ese mismo día) en vez de dejarla como variable de entorno junto a `FIREBASE_SERVICE_ACCOUNT_AGORA`; (3) `calendarId: 'primary'` no sirve para una cuenta de servicio — ese literal siempre resuelve al calendario propio (vacío) de la cuenta de servicio, nunca al calendario compartido de `letiende.co@gmail.com`, corregido reemplazándolo por el correo real; (4) causa raíz final, no de código — la **Google Calendar API nunca había sido habilitada** en el proyecto de Google Cloud `comandante-letiende` (paso documentado en `docs/tareas-a-realizar.md` §10 que no se había ejecutado realmente pese a estar marcado), diagnosticado con un logging de error permanente agregado a `sincronizarEventoCalendar()` (mensaje, código HTTP y cuerpo de error real de Google — decisión explícita de dejarlo así de forma permanente, no revertirlo, por ser información segura de loguear y de valor para observabilidad futura). 396 pruebas backend en verde. **PR #48 fusionado (25/08/2026)** — desplegado automáticamente a `production` por el push a `main`, verificado por CLI (`aws lambda get-function-configuration`/`aws ssm get-parameter` contra `agora-letiende-production-eventos`): la Lambda real de producción quedó con la misma credencial válida (`agora-calendario@comandante-letiende.iam.gserviceaccount.com`) ya en SSM, sin necesitar ningún paso adicional — la Google Calendar API habilitada en el proyecto `comandante-letiende` (25/08/2026) es una configuración a nivel de proyecto, no por stage, así que ya cubre `production` también.

**Bold — Sub-tarea 2 (frontend, roadmap #19): tres rondas de bugs/refinamientos reales en staging (26/08/2026):** **Ronda 1** — la primera implementación (widget declarativo `<script data-bold-button>`) nunca mostraba el botón: la librería de Bold escanea el DOM buscando el marcador **una sola vez, de forma síncrona**, y la implementación lo insertaba demasiado tarde. Reescrita con la API oficial `window.BoldCheckout` (`developers.bold.co/pagos-en-linea/boton-de-pagos/integracion-manual/integracion-personalizada`) — de paso cumple el acuerdo original de un botón 100% con estilo Le Tiende. **Ronda 2** — con el botón ya visible, el checkout embebido se completaba bien pero al terminar la página no reaccionaba de ninguna forma (Bold no ofrece, ni documentado ni observado en la práctica, un mecanismo confiable para notificarlo). Se agregó un botón manual "¿Ya pagaste? Verifica el estado aquí". **Ronda 3** — el usuario confirmó (y se verificó contra `developers.bold.co/webhook`) que **el webhook de Bold es automático en producción** — el paso de anotar la URL del webhook dentro del checkout embebido es exclusivo del modo sandbox de pruebas. Se rediseñó la tarjeta a una máquina de estados con un único botón visible a la vez: "Pagar con Bold" → checkout abierto (sin botones, mensaje de espera) → al detectar el cierre del modal (vía el `postMessage` interno `BOLD_CHECKOUT_EVENT` de Bold, filtrado por origen y tipo, usado solo como disparador de una reconsulta de lectura) → si sigue `esperando_pago_bold` (inconcluso — Bold nunca notifica un abandono sin pago, confirmado contra la documentación), aparecen **"Verificar estado"** y **"Reabrir el pago con Bold"** (misma instancia de `checkoutBold()`, sin nueva reserva de aforo). Dos condiciones de carrera reales encontradas y corregidas en el proceso (navegar a otro evento mientras la librería o la consulta de estado estaban en curso). Selector de medio de pago cuando el evento ofrece transferencia y Bold a la vez. Detalle completo en `docs/MEMORY.md` §9. **PR #51, todavía sin fusionar** — build y 313 pruebas frontend en verde (432 backend sin cambios, sanity check). **Falta que el usuario repita la validación manual completa en staging** con las tarjetas de prueba de Bold (aprobada/rechazada, modo sandbox), cerrando el modal sin pagar para confirmar "Verificar"/"Reabrir", y el botón "Test the webhook" del panel de Bold.

**Bold — Sub-tarea 1 (backend, roadmap #19) completa y verificada de punta a punta en staging real (25/08/2026, continuación). PR #50 (ya fusionado en `main`, corrección de una nota equivocada que quedó sin actualizar tras la fusión real — ver `docs/MEMORY.md` §9):** implementación completa (commit `3bf0211`) — `server/api/services/bold.ts` (firma saliente SHA256 del botón, verificación HMAC-SHA256 timing-safe del webhook con rama de llave vacía en modo pruebas), `server/api/handlers/bold-webhook.ts` (`POST /api/pagos/bold/webhook`, idempotente vía `ConditionExpression`), nuevo estado `EstadoCompra: 'esperando_pago_bold'`, nueva rama `medioPago: 'bold'` en `crearCompra()`, reconciliación defensiva en `aforo.ts` (`esSeguroExpirarReservaBold`) antes de expirar una reserva de Bold — resolvió el riesgo abierto que el plan había dejado explícito sobre el bloqueo del `payment_id`, consultando directamente por `compraId` (`is_external_reference=true`) sin necesitar guardarlo de antemano. Función Lambda `boldWebhook` con rol IAM propio de mínimo privilegio; checklist de `deploy.yml` aplicado desde el primer commit. 432 pruebas backend en verde. Fix propio antes de la revisión (commit `54e65d9`): `ventasEfectivo` también necesitaba `BOLD_LLAVE_IDENTIDAD` en su `environment` para la reconciliación. **Revisión de seguridad independiente:** riesgo LOW, 0 hallazgos críticos/altos; encontró y se corrigió un MEDIUM antes del PR (commit `a10ada8`) — la reconciliación con Bold sin timeout propio, en el camino síncrono de `reservarSillas()`, podía agotar el timeout completo del Lambda y bloquear la compra de OTRO cliente compitiendo por la misma silla; corregido con `AbortSignal.timeout(3000)` y `Promise.allSettled` para paralelizar las verificaciones. **GitGuardian** marcó un falso positivo en `bold.spec.ts` (commit `900f0db`) — el valor de prueba es el ejemplo público de la propia documentación de Bold, no una credencial real; se agregó una regla de ignorado explícita y auditable en `.gitguardian.yaml`, confirmada por el usuario en el panel. **Verificación real de punta a punta contra staging** (no solo pruebas mockeadas): evento de prueba temporal en DynamoDB, 5 escenarios ejercitados por `curl`/AWS CLI contra el API real — compra con `medioPago: bold` reserva aforo real y devuelve firma verificada a mano; webhook `SALE_APPROVED` firmado con HMAC-SHA256 real emite 2 boletas y confirma aforo; reenvío del mismo webhook no duplica boletas (idempotencia real); webhook con firma inválida responde `401` sin tocar la base de datos; `SALE_REJECTED` libera el aforo correctamente. CloudWatch sin errores inesperados. Todos los datos de prueba se limpiaron de staging al terminar. Ver `docs/MEMORY.md` §9 para la narrativa completa.

---

## Tarea 1 — Bold, Sub-tarea 2: frontend — roadmap #19, validación manual pendiente

**Prioridad Alta**, v2. **Implementación completa** (código, build y pruebas en verde) — ver "Estado al cierre de sesión" arriba y `docs/MEMORY.md` §9. **PR #51 (Sub-tarea 2), todavía sin fusionar**, sobre `PR #50 (Sub-tarea 1, backend), también todavía sin fusionar`. Plan completo en `.omc/plans/bold-pagos.md` (§3.3/§5/§7).

**Ya implementado:** en `comprar.component.ts`/`.html`, cuando el evento ofrece Bold como medio de pago, un selector (solo visible si también ofrece transferencia) y una máquina de estados con **un único botón visible a la vez** dentro de la tarjeta `esperando_pago_bold`: (1) "Pagar con Bold" (clases exactas de Le Tiende, invoca `window.BoldCheckout` — API oficial, `renderMode: 'embedded'` — instanciado con los datos que ya devuelve `crearCompra()`, nunca hardcodeados ni calculados en el cliente); (2) al hacer click, se ocultan todos los botones y aparece un mensaje de espera mientras el checkout está abierto; (3) al detectar el cierre del modal (vía el `postMessage` interno `BOLD_CHECKOUT_EVENT` de Bold, filtrado por origen y tipo — no documentado oficialmente, usado solo como disparador de una reconsulta de lectura, nunca como fuente de verdad) se reconsulta automáticamente `GET /api/compras/:compraId/estado`; (4) si el resultado sigue siendo `esperando_pago_bold` (inconcluso — Bold no reporta ningún evento de webhook para un abandono sin pago, verificado contra `developers.bold.co/webhook`), aparecen **"Verificar estado"** (reconsulta manual) y **"Reabrir el pago con Bold"** (misma instancia, sin nueva reserva de aforo). (Nota histórica: la primera versión usaba el widget declarativo `<script data-bold-button>` de Bold, que nunca llegó a renderizar nada por un bug real de temporización — ver `docs/MEMORY.md` §9 para el diagnóstico completo antes de tocar este mecanismo de nuevo.) El regreso automático vía redirección de Bold (si llega a ocurrir) tampoco confía en `bold-tx-status` de la URL — siempre reconsulta el backend. Sin polling activo (mismo patrón que "esperando aprobación" hoy). Pantallas nuevas para `rechazada`/`expirada` con botón "Intentar de nuevo".

**Se completa cuando:** el usuario valide manualmente en staging con las tarjetas de prueba de Bold (aprobada y rechazada, modo sandbox) más el botón "Test the webhook" del panel de Bold (el webhook no se dispara solo en modo pruebas, ver §2.4 del plan) — completando desde la interfaz real la validación de punta a punta que la Sub-tarea 1 ya hizo por API directa (ver `docs/MEMORY.md` §9) — y fusione el PR #51 (PR #50 ya fusionado). **Depende también del PR #52** (rama `fix/aforo-esperando-pago-bold`, independiente): sin ese fix, una reserva Bold vencida sigue reportándose como `esperando_pago_bold` en vez de `expirada`, lo que hace que "Verificar estado" nunca refleje la expiración real de una compra abandonada — encontrado y corregido durante la validación de esta misma sub-tarea (ver `docs/MEMORY.md` §9).

---

## Tarea 2 — Exportación de reportes en PDF (roadmap #21, resto)

**Prioridad Media**, v2 — la que le corresponde al algoritmo normal del motor JIT (comparar `PRD.md` roadmap contra `MEMORY.md` estado, sin bloqueo externo conocido). De los ítems de v2 que quedan, es el único sin ningún prerrequisito externo: WhatsApp (#20) sigue bloqueado por la Verificación de Negocio de Meta, sin cambios desde la sesión anterior; Bold (#19) ya se activó arriba pese a su bloqueo, por decisión explícita del usuario; Google Calendar (#22) ya está completo (ver arriba). La Exportación XLSX (roadmap #21) ya está implementada y fusionada desde v1 — el PDF nunca se inició.

**Alcance:** `docs/PRD.md` §6 (tabla de roadmap v2) lista "Exportación de reportes en XLSX y PDF" con XLSX ya entregado; el motor de generación de PDF queda por definir (`CLAUDE.md` §2 lo deja explícitamente "por definir"). Ampliación prevista de `handlers/reportes.ts` (`docs/tech-specs.md` §11 fila 21), reutilizando el patrón ya probado de exportación XLSX (autorización por rol/pertenencia al evento, enlace de descarga prefirmado y de vida corta — datos personales, `CLAUDE.md` §5 sección Habeas Data).

---

## Backlog

Vacío de ítems v1 (`PRD.md` §6) — Panel de control básico fue el último. Exportación XLSX (roadmap #21), fix de `etapaId` y Etapas de boletería con cierre automático (roadmap #23) **fusionados** (PR #25/#26/#28). `docs/plan-pre-produccion.md` (8 tareas técnicas) **completo y fusionado**. Tres hotfixes antes del paso a producción y segunda ronda de hotfixes **fusionados** (PR #41/#42). **Dominio personalizado `agora.letiende.co` fusionado y verificado en vivo (PR #43, ADR-013)** — roadmap #17 completo. **Boletería opcional (roadmap #24) fusionada (PR #46).** **Eventos con boletería externa (roadmap #25) fusionada (PR #47).** **Sincronización con Google Calendar (roadmap #22) fusionada y verificada también en producción por CLI (PR #48).** De v2 (roadmap #19-21): Bold (#19) — Sub-tarea 1 (backend) completa y verificada de punta a punta en staging real, **PR #50 todavía sin fusionar**; Sub-tarea 2 (frontend) activa como Tarea 1 para la siguiente sesión. Exportación PDF (#21, resto) activa como Tarea 2; WhatsApp (#20) sigue bloqueado por prerrequisito externo (ver "Pendientes que no son de código" abajo).

---

## Pendientes que no son de código

No ocupan slots del motor JIT porque no dependen del desarrollo. **El paso a paso completo está en `docs/tareas-a-realizar.md`** (documento de trabajo personal de OCM, fuera de control de versiones porque puede contener secretos).

Lo que bloquea el primer evento real, pero no el desarrollo inmediato:

- ✅ Remitente `taquilla@letiende.co` en SES probado — llegó a bandeja de entrada en Gmail (sección 5). Queda una verificación opcional de cabeceras SPF/DKIM/DMARC, no bloqueante.
- ✅ Cuenta de servicio de Firebase creada y cargada (sección 4.3, ADR-002). **No se registra app web propia** — Ágora reutiliza el `firebaseConfig` de Comandante, igual que Babel (ADR-010). El dominio `agora.letiende.co` ya está en Authorized domains; falta el de staging, que se agrega cuando se monte el dominio personalizado (roadmap #11 del backlog).
- ✅ `SES_REMITENTE`, `URL_BASE_APP` y `SECRETO_ENLACES_MAGICOS` creados en GitHub (`staging`, 08/08/2026) — el correo con el enlace de comprobante llega correctamente, verificado en vivo por el usuario. **Confirmado también en `production` (14/08/2026, auditoría pre-lanzamiento):** los 4 secretos por-ambiente existen en el entorno `production` de GitHub — `SES_REMITENTE` tenía un espacio al inicio, corregido con `gh secret set`.
- ✅ **`agora-usuarios-production` tenía al menos un `administrador` (14/08/2026, auditoría pre-lanzamiento) — estaba vacía, se agregó `ocastelblanco@gmail.com`.** Sin esto, nadie podía operar el sistema en producción pese a tener sesión válida (proyecto Firebase compartido, autorización de Ágora independiente, `CLAUDE.md` §5 A01).
- ✅ `SECRETO_FIRMA_BOLETAS` creado en GitHub (`staging` **y** `production`, 09/08/2026) — valores distintos por entorno, generados aleatoriamente (256 bits). Wire-up en `deploy.yml` y en la Lambda que lo consume ya completado como parte de Emisión de boletas (PR #19, `MEMORY.md` §2) — bullet dejado como referencia histórica del secreto, no una tarea pendiente.
- ✅ `GOOGLE_CALENDAR_SERVICE_ACCOUNT` creado en GitHub (`staging` **y** `production`, ambos ya confirmados) y conectado al deploy en ambos jobs (`f6a8989`) — la credencial real vive en SSM Parameter Store (`/agora/${stage}/google-calendar-service-account`), no como variable de entorno de Lambda. **Google Calendar API habilitada en el proyecto `comandante-letiende` (25/08/2026)** — causa raíz final del bloqueo de esta tarea, resuelta por el usuario desde la consola de Google Cloud (configuración a nivel de proyecto, cubre `staging` y `production` por igual). **PR #48 fusionado y verificado en producción por CLI el mismo día** — sin pendientes.

Fase 2, conviene arrancarlo pronto porque la Verificación de Negocio de Meta es lenta:

- 🟢 Alta de la WABA de WhatsApp (sección 8). Ojo con el requisito del número: **no puede estar en uso en la app de WhatsApp**.
- ✅ Llaves de Bold (sección 9) — obtenidas por el usuario (`staging` **y** `production`), webhooks configurados en el panel de Bold. Ya no bloquea nada; bullet dejado como referencia histórica del secreto.

**Recordatorio activo (no es una tarea, ver `MEMORY.md` §9):** revisar el costo diario real de la cuenta AWS 48 horas después del primer despliegue a staging (el 04/08/2026 o después) — `aws ce get-cost-and-usage`, `docs/advertencia-urgente-costos-aws.md` §4 Paso 4.

**Recordatorio activo (14/08/2026):** confirmar que el costo del dominio personalizado de producción (ADR-013, se verificó teóricamente en US$0 adicional) se sostiene en la factura real 48 horas después del despliegue del PR #43 (16/08/2026 o después) — mismo comando que arriba.

**Recordatorio activo (nuevo, 25/08/2026):** revisar el costo real de SSM Parameter Store 48 horas después del despliegue a producción del PR #48 (27/08/2026 o después) — se verificó teóricamente como gratis (nivel Standard, sin "higher throughput") el mismo día de la implementación, falta confirmarlo en la factura real — mismo comando que arriba.
